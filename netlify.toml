[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  GRAPHQL_URL = "https://jagger-app/graphql"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
